name: Build

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main

jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: SonarQubeScan
        uses: SonarSource/sonarqube-scan-action@v5.3.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL }}
          SONAR_PROJECTKEY: export-doc-sire-ppe-service

          SONAR_SCANNER_OPTS: -Xmx2G -Dsonar.scanner.skipJreProvisioning=true -Dsonar.verbose=true
        with:
          args: >
            -Dsonar.projectKey="${SONAR_PROJECTKEY}" -Dsonar.buildString=${{ github.run_id }}
